/**
 * SiteSpark Frontend Configuration
 * ──────────────────────────────────
 * Set API_BASE to your Render backend URL.
 * Update this AFTER deploying the backend to Render.
 */
const API_BASE = '';
// ↑ After Render deploy, change to: 'https://your-app-name.onrender.com'
// Leave empty string for local development (same-origin requests)
